Build / release-track pill attached to a platform card header.

```jsx
<Pill tone="neutral">Upload build</Pill>
<Pill tone="ready" icon={<Check />}>MyGame-1.4.ipa</Pill>
<Pill tone="processing" icon={<Spinner tone="processing" />}>Analyzing…</Pill>
```

1.5px border, 5px radius — a pill is an outline, not a fill.
