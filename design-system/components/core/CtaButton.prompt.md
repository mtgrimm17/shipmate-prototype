The one big green button — splash/marketing pages only, never inside the app shell.

```jsx
<CtaButton>GET STARTED</CtaButton>
```

Fixed 320×68. Uppercase copy, IBM Plex Mono 600 at 25px with 0.08em tracking.
