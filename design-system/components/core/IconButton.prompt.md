Square 40px icon button — topbar and project-bar affordances.

```jsx
<IconButton label="Edit game details"><PencilIcon /></IconButton>
```

Rest state is a 3% white well with a transparent border; hover swaps to panel-2 with a visible hairline. Put a 16px stroke-1.8 SVG inside.
