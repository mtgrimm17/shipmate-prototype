```jsx
<NoticeBanner onDismiss={hide}>
  Do you already have a marketing plan? or press strategy? Contact these fine partners top of their class and reach even more
</NoticeBanner>
```

Yellow on dark ink. If the message can't be dismissed it isn't a notice — use `AlertBox` or an `ActionCard`.
