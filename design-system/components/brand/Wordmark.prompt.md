```jsx
<Wordmark height={40} />
```

The lockup carries its own black lozenge, so it only sits cleanly on `--bg` or `--bg-marketing`. In tight spots (modal headers) the product falls back to the word "Shipmate" in Inter 700, 15px, 0.04em, uppercase.
